import model from 'modules/checkroll/checkrollModel';

const { fields } = model;

export default [
  fields.workerID,
  fields.workcode,
  fields.no,
  fields.quantity,
  fields.block,
  fields.month,
  fields.week,
  fields.year,
];
