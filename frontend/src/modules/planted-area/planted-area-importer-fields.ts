import { PlantedAreaModel } from '@/modules/planted-area/planted-area-model';

const { fields } = PlantedAreaModel;

export default [
  fields.location,
  fields.block,
  fields.plantedHa,
  fields.totalPalm,
];
